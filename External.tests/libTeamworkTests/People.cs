using Microsoft.VisualStudio.TestTools.UnitTesting;
using Moq;
using RestSharp;

namespace External.tests.libTeamworkTests
{
    [TestClass]
    public class People
    {

        [TestInitialize]
        public void Initialize()
        {
            /*
            Mock<RestRequest> mockRestRequest = new Mock<RestRequest>();
            Mock<RestClient> mockRestClient = new Mock<RestClient>();
            mockRestClient.Setup(x => x.Execute(It))
            */
        }
    }
}